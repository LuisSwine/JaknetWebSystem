<%- include('../partials/head.ejs', {tituloPagina: 'Asignar Roles'}) %> 
<%- include('../partials/linksNav.ejs')%> 
<%- include('../partials/header.ejs', {user: user}) %> 

<div class="content-wrap">
    <div class="main">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-8 p-r-0 title-margin-right">
                    <div class="page-header">
                        <div class="page-title">
                            <h1>Bienvenido <%= user.nombres%></h1>
                        </div>
                    </div>
                </div>
                <!-- /# column -->
                <div class="col-lg-4 p-l-0 title-margin-left">
                    <div class="page-header">
                        <div class="page-title">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
                                <% if(flag == 1){%>
                                    <li class="breadcrumb-item"><a href="/profileProyect?proyecto=<%= proyecto[0].folio %>&cliente=<%= cliente %>&flag=<%= flag %>"><%= proyecto[0].nombre %></a></li>
                                <%}else if(flag == 3 || flag == 2){%>
                                    <li class="breadcrumb-item"><a href="/profileProyect?proyecto=<%= proyecto[0].folio %>&ubicacion=<%= ubicacion %>&cliente=<%= cliente %>&flag=<%= flag %>"><%= proyecto[0].nombre %></a></li>
                                <%}else if(flag == 0){%>
                                    <li class="breadcrumb-item"><a href="/profileProyect?proyecto=<%= proyecto[0].folio %>&flag=<%= flag %>"><%= proyecto[0].nombre %></a></li>
                                <%}else if(flag == 4){%>
                                    <li class="breadcrumb-item"><a href="/profileProyect?proyecto=<%= proyecto[0].folio %>&flag=<%= flag %>&permisos=<%= permisos %>"><%= proyecto[0].nombre %></a></li>
                                <%} %>
                                <li class="breadcrumb-item active">Asignar Roles</li>
                            </ol>
                        </div>
                    </div>
                </div>
                <!-- /# column -->
            </div>
            <section id="main-content">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-title">
                            <h4>ASIGNAR A UN USUARIO UN ROL</h4>
                        </div>
                        <div class="card-body">
                            <div class="basic-elements">
                                <form action="/asignRol" method="post">
                                    <div class="row">
                                        <div class="col-lg-6">
                                            <div class="form-group">
                                                <label>USUARIO</label>
                                                <select class="form-control" name="usuario" id="usuario">
                                                    <option selected disabled>--Seleccione--</option>
                                                    <% usuarios.forEach((usuario)=>{ %>
                                                    <option value="<%= usuario.folio %>"><%= usuario.nombres %> <%= usuario.apellidos %></option>
                                                    <% }) %> 
                                                </select>
                                            </div>
                                            <div class="form-group">
                                                <input type="number" class="form-control" value="<%= proyecto[0].folio %>" id="proyecto" name="proyecto" hidden>
                                                <input type="number" class="form-control" value="<%= ubicacion %>" id="ubicacion" name="ubicacion" hidden>
                                                <input type="number" class="form-control" value="<%= cliente %>" id="cliente" name="cliente" hidden>
                                                <input type="number" class="form-control" value="<%= flag %>" id="flag" name="flag" hidden>
                                                <input type="number" class="form-control" value="<%= permisos %>" id="permisos" name="permisos" hidden>
                                            </div>
                                            <button type="submit" class="btn btn-primary m-b-10">ASIGNAR ROL</button>
                                        </div>
                                        <div class="col-lg-6">
                                            <div class="form-group">
                                                <label>ROL</label>
                                                <select class="form-control" name="rol" id="rol">
                                                    <option selected disabled>--Seleccione--</option>
                                                    <% roles.forEach((rol)=>{ %>
                                                    <option value="<%= rol.folio %>"><%= rol.rol %></option>
                                                    <% }) %> 
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>  
            </section>
            
        </div>
    </div>
</div>

<%- include('../partials/scriptsNav.ejs') %> 
<%- include('../partials/footer.ejs') %> 
